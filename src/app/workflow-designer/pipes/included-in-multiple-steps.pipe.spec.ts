import { IncludedInMultipleStepsPipe } from './included-in-multiple-steps.pipe';

describe('IncludedInMultipleStepsPipe', () => {
  it('create an instance', () => {
    const pipe = new IncludedInMultipleStepsPipe();
    expect(pipe).toBeTruthy();
  });
});
